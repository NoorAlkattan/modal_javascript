<%= form_for(first_page) do |f| %>
  <% if first_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(first_page.errors.count, "error") %> prohibited this first_page from being saved:</h2>

      <ul>
      <% first_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
